import { StyleSheet } from 'react-native';
import COLORS from '../../../styles/colors';

const styles = StyleSheet.create({
  accordionItem: {
    backgroundColor: COLORS.SUB_ITEM,
  },
  itemText: {
    color: COLORS.TEXT,
  },
  listItem: {
    backgroundColor: COLORS.SUB_ITEM_ITEM,
  },
});

export default styles;
